# 我的极简博客

基于 Astro + Starlight 构建的个人博客,托管于 GitHub Pages,记录技术学习与生活。站点内容分为常规博客文章与一个自动同步的 Steam 游戏画廊。

## Language

**游戏画廊(Gallery)**:
以 Notion 画廊风格网格展示用户 Steam 游戏数据的页面,每张卡片含封面、名称与游玩时长。
_Avoid_: 游戏机房、游戏墙、游戏列表

**游戏数据(SteamGame)**:
从 Steam Web API 抓取的单个游戏条目,含应用 ID(appid)、名称、总游玩时长、近两周时长与封面图。
_Avoid_: 游戏记录、游戏项

**游玩时长(Playtime)**:
以小时计数的游戏游玩时间。区分**总时长**(playtime_forever,全部历史累计)与**近两周时长**(playtime_2weeks,滚动 14 天窗口)。
_Avoid_: 游戏时间、小时数

**数据同步(Sync)**:
GitHub Actions 定时抓取 Steam Web API、生成数据文件并触发站点重新部署的自动化过程。每日执行,失败时保留上一次成功的数据。
_Avoid_: 更新、刷新、拉取

**封面(Cover)**:
游戏在画廊卡片中展示的 2:3 竖版封面图,直接来自 Steam CDN,构建期无需下载到本地。
_Avoid_: 图片、海报、封面图资源

**数据文件(games data)**:
站点构建时读取的本地 JSON 文件(位于 `public/steam_games.json`),是游戏画廊唯一的数据源。
_Avoid_: 数据库、数据源文件

**游玩状态(Play Status)**:
用户与一款游戏的游玩关系标记,六阶梯:**未通关**(uncompleted)、**已通关**(completed)、**全成就**(perfect)、**持续游玩**(ongoing,竞技/多人游戏或通关后仍长期在玩)、**暂退长草**(hiatus,暂时不玩但可能回归)、**已退役**(retired,彻底不玩了),以胶囊标签形式展示。
_Avoid_: 状态、进度、完成度

**段位(Rank)**:
竞技/网络游戏中的历史最高段位或线上战绩,自由文本(如 `璀璨钻石 💎`、`线上150级 🚗`),存于 `my_rank` 字段。有段位的游戏,段位徽章与游玩状态徽章并排展示(段位只追加、绝不顶替状态)。
_Avoid_: 排名、等级

**短评(Review)**:
用户写在本网站上的对单款游戏的简短评价,保存在注释文件中,为空时不展示。
_Avoid_: 评论、评价、心得

**长评(Deep Review)**:
用户写在本网站博客部分的完整评测文章,通过 `blog_url` 字段从气泡面板链接过去。
_Avoid_: 深度评测、文章

**注释文件(annotations)**:
用户手工维护的本地 JSON 文件(位于 `src/data/steam_annotations.json`),按 appid 记录游玩状态、段位、短评、标签、长评链接等不可再生的手写数据,构建期与数据文件合并。同步脚本永不触碰。
_Avoid_: 用户数据、手工数据、标注

**标签(Tags)**:
游戏的特征词(如魂系、开放世界、RPG),用于气泡面板与卡片标记。主要来自用户手写,可由元数据丰富脚本以官方 genres 预填建议值。
_Avoid_: 类型、分类、Tag 区域

**Overview 列表(Overview list)**:
画廊页面右侧的目录列表,一行一款游戏展示标题、游玩状态与总时长,点击游戏名可滚动定位并选中对应的画廊卡片,与卡片双向高亮联动。
_Avoid_: 目录、导航、侧栏

**发售日期(Release Date)**:
游戏在 Steam 商店的官方发售日期,由元数据丰富脚本从 appdetails 自动抓取,完整日期存于注释文件的 `release_date` 字段,卡片上仅展示提取出的年份。用户手写的 `play_year`(游玩年份)与之不同。
_Avoid_: 发售时间、上架日期
