# 手工数据与自动数据分离存储,构建期合并

通关状态、短评、标签、长评链接等用户手写数据存于 `src/data/steam_annotations.json`(按 appid 索引),自动抓取的数据存于 `public/steam_games.json`,组件构建期读取两个文件合并为最终展示数据。

原因:每日同步脚本全量覆盖数据文件,若手写数据混入其中会在下次同步时被冲掉。手写数据不可再生,自动数据随时能重抓,分离存储让同步脚本保持纯粹、永不触碰用户内容,零丢失风险。
